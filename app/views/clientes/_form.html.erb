<%= form_with(model: cliente) do |form| %>
  <% if cliente.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
        <% cliente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :nombre, style: "form-label" %>
    <%= form.text_field :nombre %>
  </div>

  <div class="mb-3">
    <%= form.label :edad, style: "form-label" %>
    <%= form.text_field :edad %>
  </div>

  <div>
    <%= form.submit "Actualizar cliente", class:"btn btn-outline-secondary" %>
  </div>
<% end %>
