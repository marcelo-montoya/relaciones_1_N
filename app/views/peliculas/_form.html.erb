<%= form_with(model: pelicula) do |form| %>
  <% if pelicula.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pelicula.errors.count, "error") %> prohibited this pelicula from being saved:</h2>

      <ul>
        <% pelicula.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :nombre, style: "form-label" %>
    <%= form.text_field :nombre %>
  </div>

  <div class="mb-3">
    <%= form.label :cliente_id, style: "form-label" %>
    <%= form.collection_select :cliente_id, @clientes, :id, :nombre %>
  </div>

  <div>
    <%= form.submit "Actualizar película", class:"btn btn-outline-secondary" %>
  </div>
<% end %>
